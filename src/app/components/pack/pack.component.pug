div([ngClass]="pack.available ? 'pack' : 'pack pack_disabled'")
  p([ngClass]="pack.available ? 'pack__text' : 'pack__text pack__text_disabled'") Сказочное заморское яство
  h2([ngClass]="pack.available ? 'pack__title' : 'pack__title pack__title_disabled'") Нямушка
  p([ngClass]="pack.available ? 'pack__consist' : 'pack__consist pack__consist_disabled'") {{ pack.consist }}
  p("*ngFor"="let val of pack.detailed"
    "[ngClass]"="pack.available ? 'pack__detailed' : " +
  "'pack__detailed pack__detailed_disabled'")
    span([ngClass]="pack.available ? 'pack__detailed pack__detailed_num' :" +
    "'pack__detailed pack__detailed_disabled pack__detailed_num'") {{val.num}}
    | {{ val.num ? ' ' + val.text : val.text }}
  div([ngClass]="pack.available ? 'pack__oval' : 'pack__oval pack__oval_disabled'")
    p(class="pack__mass") {{ pack.mass.toString().split('.').join(',') }}
    p(class="pack__mass pack__mass_measure") кг
  div(class="pack__info")
    p(*ngIf="pack.available" class="pack__info-text") Чего сидишь? Порадуй котэ,&nbsp;
      span(class="pack__info-text pack__info-text_btn") купи.
    p(*ngIf="!pack.available" class="pack__info-text pack__info-text_disabled") Печалька, {{ pack.consist }} закончился.
